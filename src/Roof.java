import java.awt.*;

// Roof so that a player can't go over all of the pipes
public class Roof extends GameBound{
    public Roof(int x, int y, double width, int height, Color color) {
        super(x, y, width, height, color);
    }
}
